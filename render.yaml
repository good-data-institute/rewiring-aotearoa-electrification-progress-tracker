services:
  - type: web
    name: electrification-dashboard
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    # Render provides PORT automatically - no need to set it
    # The dashboard will be accessible at the Render URL
